namespace Smoower.Minified.Tests;public class ForbiddenTokenCheckerTests{private static readonly string[] Forbidden =["[HttpGet", "[HttpPost", "[HttpPut", "[HttpPatch", "[HttpDelete","[Route(", "[ApiController", ": ControllerBase", ":ControllerBase","IActionResult", "ActionResult",".Where(", ".Select(", ".ToListAsync(", ".FirstOrDefaultAsync(",".SaveChangesAsync(", "///", "#region", "#endregion",];public static IEnumerable<object[]> SampleFiles(){var dir = FindSamplesDir();foreach (var file in Directory.EnumerateFiles(dir, "*.cs", SearchOption.AllDirectories)){if (file.Contains($"{Path.DirectorySeparatorChar}obj{Path.DirectorySeparatorChar}") ||file.Contains($"{Path.DirectorySeparatorChar}bin{Path.DirectorySeparatorChar}"))continue;if (file.Contains($"{Path.DirectorySeparatorChar}Generated{Path.DirectorySeparatorChar}"))continue;if (file.EndsWith(".min.cs", StringComparison.OrdinalIgnoreCase))continue;if (file.Contains("Individual-Samples"))continue;if (file.Contains($"{Path.DirectorySeparatorChar}TodoApi{Path.DirectorySeparatorChar}"))continue;if (Path.GetFileName(file).Equals("GlobalUsings.cs", StringComparison.OrdinalIgnoreCase))continue;yield return [file];}}[Th][Mem(nameof(SampleFiles))]public void SampleFile_HasNoForbiddenLongFormTokens(string file){var text = File.ReadAllText(file);var hits = Forbidden.Where(text.Contains).ToArray();(hits.Length == 0).tru($"{Path.GetFileName(file)} contains forbidden long-form token(s): {string.Join(", ", hits)}");}private static string FindSamplesDir(){var dir = new DirectoryInfo(AppContext.BaseDirectory);while (dir is not null){var samples = Path.Combine(dir.FullName, "samples");if (Directory.Exists(samples))return samples;dir = dir.Parent;}throw new DirectoryNotFoundException("Could not locate the 'samples' directory above the test output folder.");}}
